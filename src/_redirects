# Redirect domain aliases to primary domain
# https://jumbocreative.co/* https://jumboxr.com/:splat 301!

# Optional: Redirect default Netlify subdomain to primary domain
# https://etribe-web.netlify.app/* https://etribe.com/:splat 301!